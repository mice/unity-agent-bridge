# Lua GC Lint Runtime

This directory contains the package-contained Lua GC lint runtime used by the built-in LuaTools plugin.

The Windows x64 product path is:

```text
out/win-x64/lua-gc-lint.exe
```

The bundled MVP payload is the approved `lua-gc-lint` Windows x64 release artifact.

Payload provenance:

- Source: `D:\ProjCommon\lua_gc_linter\dist\lua-gc-lint-v0.1.0-win-x64\lua-gc-lint.exe`
- SHA256: `70C04AD8EF682881BA1BE5F5CC4D49EE60579556324C7B7D66CC7E328A1CC5AB`

Only the approved runtime payload under `out/win-x64/` should be distributed from this directory.
